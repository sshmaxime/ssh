import { styled } from "@mui/material/styles";

const style = {
  BodyScene: styled("div")(({ theme }) => ({
    height: "100%",
    width: "100%",
    position: "absolute",
    top: 1,
  })),
};

export default style;
